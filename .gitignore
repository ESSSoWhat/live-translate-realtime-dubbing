# Build output
build/
dist/
# Allow PyInstaller spec files (build config)
!*.spec

# FFmpeg (embedded repo - use submodule or system install)
ffmpeg/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.eggs/
venv/
.venv/
env/

# Environment
.env
*.env.local

# Node
node_modules/

# IDE
.vscode/
*.code-workspace
*.bak
*.orig

# Windows
nul
Thumbs.db
Desktop.ini

# Claude
.claude/settings.local.json

# Cursor
.cursor/

# Python type checker
.mypy_cache/

# Dart / Flutter
.dart_tool/
.flutter-plugins-dependencies
# Local Flutter SDK checkout (use PATH / FVM / official install)
flutter/

# Android
**/android/.gradle/
**/android/app/.cxx/
**/.cxx/
**/local.properties
# Eclipse / Buildship / IDE (machine-specific java.home, init scripts, etc.)
# Do not commit — see android/README.md
**/android/.settings/
**/android/app/.settings/

# Local JDK installs / archives (use Android Studio JBR, Temurin 17, or CI)
JDK/

# Pub cache if created inside the repo (prefer global PUB_CACHE)
mobile/.pub-cache/

# Root-level Flutter Windows ephemeral output (non-standard layout)
windows/flutter/ephemeral/

# Large binary files (exceed GitHub 100MB limit)
*.pdb